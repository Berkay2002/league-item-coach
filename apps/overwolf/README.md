# Overwolf app

This workspace builds the first League Item Coach Overwolf shell.

Build it from the repo root:

```bash
bun run build --filter=overwolf
```

Load `apps/overwolf/dist` as an unpacked app in the Overwolf development environment. The built folder contains `manifest.json`, `index.html`, and bundled assets at the app root.
